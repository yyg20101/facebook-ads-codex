# 贡献指南

仓库当前以文档基线为主。开始任何变更前，必须确认目标工作已经在
[项目状态与授权](docs/project/status-and-authorizations.md)中获准。

## 开始前

1. 阅读 [`AGENTS.md`](AGENTS.md)。
2. 阅读[项目文档入口](docs/README.md)和当前任务阅读清单。
3. 检查当前阶段、需求 ID、依赖和 Gate。
4. 检查工作区并保留已有变更。
5. 确认工作不需要新的部署、Meta 写操作或 Secret 权限。

## 文档类型

| 类型 | 事实源范围 | 变更要求 |
| --- | --- | --- |
| 项目治理 | 章程、当前状态和授权 | 权限扩大需要负责人明确批准 |
| 需求 | 产品行为、质量和验收 | 稳定 ID、验收条件、追踪矩阵 |
| 技术 | 架构、数据、接口和安全设计 | 关联需求；长期决策使用 ADR |
| 规范 | 工程、测试和发布实践 | 不得削弱需求或安全政策 |
| 规划 | 阶段、Gate、问卷和证据 | 标明状态、所有者和实际证据 |
| Runbook | 目标环境操作 | 实际验证前只能是 `DRAFT` |

## 写作和结构

遵循[文档规范](docs/standards/documentation.md)：

- 简体中文描述约束，代码标识和状态保留英文。
- `docs/` 文件使用统一 front matter。
- 单文件一个一级标题，标题层级连续。
- 规范要求使用 `MUST`、`MUST NOT`、`SHOULD`、`MAY`。
- 未知事实使用 `UNRESOLVED` 和 `BQ-*`。
- 不复制授权、安全、指标或状态机的完整事实源。
- 示例不得包含真实 Token、账户标识、客户数据或可复用凭据。

## 分支与提交

- `dev` 是开发和集成分支。
- `main` 只接收项目负责人验收后的稳定基线。
- 不创建额外长期分支，除非项目负责人明确要求。
- 提交必须范围清晰，不得静默包含用户无关变更。
- 除非用户明确要求，不自行暂存、提交、推送或合并。

## 提交前检查

```bash
npm run docs:check
```

- [ ] 变更属于当前授权范围。
- [ ] 需求、设计、ADR 和规范没有冲突。
- [ ] 文档链接、元数据、ID 和追踪矩阵有效。
- [ ] 没有凭据、Token、个人数据或敏感响应。
- [ ] 相关测试和证据已记录。
- [ ] 变更日志按需更新。
- [ ] staged diff 只包含预期文件。
