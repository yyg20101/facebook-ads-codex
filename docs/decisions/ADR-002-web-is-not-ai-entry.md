---
doc_id: ADR-002
type: decision
status: ACCEPTED
owner: project_owner
last_reviewed: 2026-08-09
---

# ADR-002：前期 Web 不重复实现 AI 会话入口

> 接受依据：`EVD-023`–`EVD-025` 已确认 Web 是业务产品主体、前期 Codex 独立运行，
> 并明确否定旧 Web 内置助手假设。

## 背景

系统需要完整的素材、广告创建与管理、数据分析、测试、审批和安全控制界面。前期复制
Codex 对话能力会造成第二个 AI 入口、重复权限逻辑和行为不一致。

## 备选方案评审

| 方案 | 结果 | 理由 |
| --- | --- | --- |
| Web 承载业务对象，Codex 独立提供 AI | 采用 | 保留完整广告运营产品形态，同时避免重复会话和权限实现。 |
| Web 内置 AI 聊天作为第二入口 | 当前不采用 | 前期没有独立价值证据，且会产生重复上下文、模型和授权边界。 |
| 只提供 Codex、不建设 Web | 不采用 | 无法承载结构化业务对象、状态、图表、人工操作和审计。 |

## 决策

Web Operations Platform 负责：

- 素材中心和素材表现关系。
- Campaign、Ad Set、Ad 创建与生命周期管理。
- 投放数据、诊断报告和测试记录。
- Meta 连接和账户配置。
- 变更预览、批准、拒绝和取消。
- 审计日志、权限和 Emergency stop。

前期自然语言生成和分析入口是 Codex。网页 MUST NOT 直接调用 Meta API，也不实现独立
AI 对话。Web 可以展示或保存 Codex 生成的结构化结果，但不能把模型输出当作已授权操作。

## 后果

- Web 专注确定性业务体验，AI 边界更清晰。
- Web 与 MCP 必须共享服务端业务和授权层。
- 分析体验依赖 Codex 可用性，但同步、审批状态和安全控制不依赖 Codex 在线。

## 风险与可逆性

- 风险：Codex 与 Web 之间需要清晰的结构化交接，早期可能包含人工复制步骤；由
  `FR-022` 约束来源、状态和人工评审。
- 可逆性：高。未来可在 `PF-022` 获得价值证据后增加 Web AI，但必须复用服务端权限并
  通过新 ADR 取代当前边界。

## 影响

- 关联需求：FR-009、FR-011、SEC-003。
- Gate：G3 验证 Web 产品范围、RBAC 和敏感信息边界。
