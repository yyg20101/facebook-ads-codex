# 贡献指南

本仓库当前处于 Pre-Phase 0，主要维护项目规范和实施准备材料。代码贡献必须等到用户
明确启动对应阶段后再进行。

## 开始前

1. 阅读 [`AGENTS.md`](AGENTS.md)。
2. 完整阅读
   [`docs/FACEBOOK_ADS_CODEX_EXECUTION_SPEC.md`](docs/FACEBOOK_ADS_CODEX_EXECUTION_SPEC.md)。
3. 检查当前阶段、依赖和 Gate。
4. 检查工作区并保留已有变更。
5. 确认工作不需要新的生产部署、Meta 写操作或密钥权限。

## 文档类型

| 类型 | 说明 | 变更要求 |
| --- | --- | --- |
| 规范性 | 执行规范 | 递增版本、更新日期、记录影响 |
| 派生说明 | README、架构、路线图、术语表 | 与规范核对，冲突时不得覆盖规范 |
| 工作文档 | 问卷、计划、证据记录 | 标明状态、负责人和更新时间 |
| 运维文档 | Runbook | 必须经过实际环境验证后才能标记可用 |

## 修改执行规范

修改
[`docs/FACEBOOK_ADS_CODEX_EXECUTION_SPEC.md`](docs/FACEBOOK_ADS_CODEX_EXECUTION_SPEC.md)
时必须：

1. 递增 front matter 中的 `version`。
2. 更新 `updated_at`。
3. 在 [`CHANGELOG.md`](CHANGELOG.md) 说明变化。
4. 明确列出受影响的 ADR、接口、Gate 或安全边界；没有影响也要明确说明。
5. 同步更新受影响的派生文档。
6. 权限扩大必须取得用户明确批准。

## 文档写作规范

- 使用简体中文描述项目约束，代码标识、API 名称和状态值保留原始英文。
- 标题层级连续，单个文件只使用一个一级标题。
- 首次出现的项目术语应链接到 [`docs/glossary.md`](docs/glossary.md) 或给出定义。
- 规范性要求使用 `MUST`、`MUST NOT`、`SHOULD`、`MAY`，避免模糊表达。
- 派生文档开头应声明其权威级别和所依据的规范版本。
- 示例不得包含真实 Token、Authorization Header、客户数据或可复用凭据。
- 不创建内容重复、没有维护责任或无法验证的占位文档。

## 实现贡献

实施开始后，每个变更应：

- 只覆盖已授权阶段内的一个清晰目标。
- 关联阶段任务编号，例如 `P1-05`。
- 提供测试、审计或人工验证证据。
- 对安全、租户、数据口径和迁移影响作出说明。
- 不通过实现代码改变执行规范定义的策略。

## 提交前检查

- [ ] 变更属于当前已授权阶段。
- [ ] 工作区中的既有变更已保留。
- [ ] 文档链接和 Markdown 结构有效。
- [ ] 规范与派生文档没有冲突。
- [ ] 没有凭据、Token、个人数据或敏感请求内容。
- [ ] 相关测试或检查已运行并记录结果。
- [ ] 变更日志和版本已按需更新。

除非用户明确要求，不要自行暂存、提交、推送或创建 Pull Request。
