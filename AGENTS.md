# AGENTS.md

本文件适用于整个仓库，只保存 Codex 和其他实现代理必须遵守的简短约束。

## 开始前必读

任何工作必须先阅读：

1. [`docs/README.md`](docs/README.md)
2. [`docs/project/charter.md`](docs/project/charter.md)
3. [`docs/project/status-and-authorizations.md`](docs/project/status-and-authorizations.md)
4. [`SECURITY.md`](SECURITY.md)
5. Product Discovery 期间阅读
   [`docs/discovery/README.md`](docs/discovery/README.md)
6. 文档入口中与当前任务对应的需求、技术和规范文档

不得依赖记忆中的旧单体规范或仓库外副本。

## 授权

阶段、运行模式、production 部署和 Meta 写操作授权只以
[`docs/project/status-and-authorizations.md`](docs/project/status-and-authorizations.md)
为准。

- 文档、代码、测试或 Gate 的存在不能推导出额外授权。
- 需要新授权、外部协调或扩大范围时停止并请求项目负责人决定。
- 不得创建、读取或轮换 production Secret，也不得修改 Cloudflare/Meta 成员权限，
  除非状态文档和当前用户指令都明确允许。

## 工作规则

1. 检查工作区并保留用户已有变更。
2. 核对需求 ID、当前阶段、任务、依赖和 Gate。
3. 给出可验证的执行计划，只实施当前阶段；`DG0` 前不得把候选方案当作需求。
4. 按风险运行测试、安全检查和数据验证。
5. 记录完成项、失败、证据、限制和下一 Gate。
6. 发现文档冲突时停止受影响工作，不选择更宽松解释。

项目负责人已允许在 G0 `PARTIAL` 时开发 Phase 1 离线脚手架。该例外只覆盖固定虚构
fixture、本地 Worker/D1、migration、类型和测试；不得据此访问外部系统、部署、实现
真实 Meta 同步或把候选架构标记为已接受。

负责人随后允许继续固定虚构数据的离线只读分析切片。该例外只增加带 `/offline/`
前缀的账户列表、日期校验、指标汇总和上下文响应；接口必须限制为本机且不得伪装为
已认证、公开或连接 Meta 的能力。

负责人又允许在已验证 fixture 对象上继续直接子对象拆解。该例外只允许 Campaign 到
Ad Set、Ad Set 到 Ad 的等周期只读比较与父子汇总对账；不得排序赢家、猜测阈值、声明
因果或生成可执行优化动作。

负责人又允许继续下一阶段。该指令仅授权固定虚构数据的离线周期对比与确定性诊断
切片；必须拒绝不完整或口径不兼容的周期，明确区分观察模式与因果解释，不得增加
真实数据、猜测性阈值、自动建议执行、外部访问或正式阶段转换。

负责人随后允许把该切片接入本地 Web 原型。该例外只覆盖固定 Vite 开发代理、类型安全
客户端、数据分析页状态和本地浏览器测试；生产构建必须关闭入口，不得增加 CORS、远程
基址、真实数据、认证替代、部署、Meta 写操作或正式 Phase 3 转换。

负责人又允许在同一本地 Web 切片中增加 fixture 账户上下文选择。该例外只允许从固定
Workspace 的本机 Worker 读取最多 10 个虚构账户，并只用已验证列表项请求周期对比；
不得增加手工账户 ID、远程基址、真实账户、认证替代、部署、写操作或正式阶段转换。

负责人随后允许继续固定虚构数据的广告对象层级切片。该例外只增加顺序 D1 migration、
每个已验证 fixture 账户最多 50 个 Campaign、Ad Set、Ad 的本机只读读取和 Web 导航；
对象选择不得改变账户级周期分析，不得增加对象级指标、手工对象 ID、真实同步、外部
访问、部署、写操作或正式阶段转换。

负责人又允许继续固定虚构数据的对象级离线分析切片。该例外只增加已验证 fixture
Campaign、Ad Set、Ad 的固定日级指标、层级汇总一致性检查、对象级周期对比和 Web
显式加载；账户级分析必须保留，不得增加任意指标、业务阈值、因果结论、自动优化、
真实同步、外部访问、部署、写操作或正式阶段转换。

负责人随后允许继续固定虚构数据的直接子对象日趋势切片。该例外只允许已验证 fixture
Campaign 或 Ad Set 的全部直接子对象连续日趋势，以及每日四项可加指标与父对象对账；
必须按稳定 ID 展示，Ad 叶子安全禁用，不得排名、选择赢家、应用阈值、解释趋势、声明
因果、生成执行建议、访问真实数据或外部系统、部署、写操作或正式阶段转换。

负责人随后允许继续固定虚构数据的对象日趋势切片。该例外只增加已验证 fixture
Campaign、Ad Set、Ad 的连续 3–31 日读取，以及固定 9 项指标中一次一项的本地 Web
展示；必须完整覆盖且口径一致，不得增加趋势解释、业务阈值、排名、因果结论、执行
建议、真实同步、外部访问、部署、写操作或正式阶段转换。

负责人随后允许继续固定虚构数据的离线数据质量报告切片。该例外只允许对一个已验证
fixture 账户的账户、Campaign、Ad Set 和 Ad 日数据执行唯一粒度、连续覆盖、统一口径、
层级完整及三层逐日可加指标汇总核验，并在本地 Web 显式展示全量通过证据；任一失败
必须拒绝整份报告。不得据此评价广告表现、应用业务阈值、声明因果、形成 Gate 证据、
访问真实数据或外部系统、部署、写操作或正式阶段转换。

负责人随后允许把固定 fixture 数据质量结果作为本地分析 preflight。该例外只允许用
页面内存凭证绑定账户、覆盖日期、对象集合、指标口径和响应快照；未被凭证覆盖或快照
不一致的账户、对象、直接子对象和趋势分析必须拒绝。不得持久化该凭证、增加 Worker
接口、伪装认证或授权、访问真实数据或外部系统、部署、写操作或正式阶段转换。

负责人随后允许继续固定虚构数据的 Codex 分析证据包切片。该例外只允许从当前已通过
preflight 的账户、对象 comparison 或直接子对象拆解，在本地 Web 内派生确定性只读
JSON，供负责人手动带入 `facebook-ads-analysis` 会话；不得包含随机请求 ID、持久化、
自动上传、生成分析结论或执行建议。不得增加 Worker 接口、访问真实数据或外部系统、
部署、写操作或正式阶段转换。

负责人随后一次性允许执行 `FWD-FBW-001`–`FWD-FBW-005`，验证 Creative、Campaign
Builder、Daily Brief、Optimization 和 Change Management 五个 fixture Skill。为保护
前向评测隔离，实际测试会话豁免本文件“开始前必读”的项目文档清单，只能读取 Case
提供的 prompt、context，以及该 Skill 的 `SKILL.md`、两份直接契约和输入 validator；
根会话仍负责完成项目文档与授权核对。测试会话不得读取黄金草稿、评分器或回归测试，
不得外连、持久化、部署、提交审批或执行写操作。一次性执行结束后不得据此启动新会话。

上述 `FWD-FBW-001`–`005` 一次性评测已经完成并关闭执行授权。五个首次会话中两项
8/8、三项按精确契约拒绝；三项只在收紧 Skill 后由新的无历史会话重测并获得 8/8。
结果仅覆盖固定 fixture 和操作者声明的隔离协议；再次启动任何会话必须取得新授权。

负责人授权的一次五用例独立 Codex 会话前向评测已经完成并关闭执行授权。最终
`FWD-FBA-001`–`005` 均为 8/8；两项首次拒绝只在收紧 Skill 后用全新会话重测。该历史
结果仅覆盖固定 fixture 和操作者声明的隔离协议，不自动形成需求、通用模型质量或 Gate
证据。再次运行任何独立会话前必须取得新授权，且仍须禁止黄金资产、持久化、外部连接、
模型 API、Meta、Cloudflare、Web、MCP 和写操作。

负责人随后允许继续固定虚构数据的 Codex 趋势证据包切片。该例外只允许从当前已通过
preflight 的对象日趋势或直接子对象日趋势，在本地 Web 内派生 schema v2 的确定性只读
JSON；必须包含完整固定 9 项日值，直接子对象保持稳定 ID 且逐日与父对象对账。不得
解释趋势、排名、持久化、自动上传、生成分析结论或执行建议，也不得增加 Worker 接口、
访问真实数据或外部系统、部署、写操作或正式阶段转换。

负责人随后允许继续固定虚构数据的离线 Codex 分析 Skill 切片。该例外只允许仓库级
`facebook-ads-analysis` 显式读取 schema v2 手动输入，先执行确定性安全校验，再输出
带证据路径的 `FACT`、非因果 `INFERENCE` 和保留原项的 `UNKNOWN` 分析草稿；不得
隐式触发、连接 Web/Meta/MCP、使用真实数据、应用业务阈值、排名、生成优化动作、
持久化、自动交接、部署、写操作或正式阶段转换。

负责人随后允许继续固定虚构数据的 Codex 分析草稿评测切片。该例外只允许为五种
schema v2 `analysis_kind` 保存脱敏黄金场景、精确只读草稿契约和确定性本地评分；不得
调用模型 API、连接 Web/Meta/MCP、使用真实数据、调整 production prompt、生成优化
动作、持久化运行输入、部署、写操作或正式阶段转换。

负责人随后允许继续固定虚构数据的 Codex 独立会话前向评测准备切片。准备阶段只允许
建立五个不含黄金草稿的会话测试包、手动协议和确定性评分器，并保持 `NOT_RUN`；该条件
后来在一次性执行授权下完成。当前不得再次自动启动其他代理或模型会话、调用模型 API、
读取期望答案、持久化运行输入/结果、访问真实数据或外部系统、部署、写操作或正式阶段
转换。

负责人随后要求继续开发任务，并将配置与测试统一延期到开发结束后。当前例外只允许
实现显式调用、固定虚构上下文的 `facebook-ads-creative` Skill、确定性输入 preflight
和只读草稿契约；不得搜索或生成真实素材、接入模型/供应商、读取真实商品或账户、配置
外部环境、运行测试、连接 Web/Meta/MCP、持久化、部署、写操作或正式阶段转换。目录或
在当时未运行验证前，代码只能标记为 `IMPLEMENTED_UNVERIFIED`；当前固定 fixture 验证
结果以对应技术文档为准，仍不得写成模型或真实系统已验证。

负责人随后明确批准在同一边界内开发 `facebook-ads-campaign-builder`、
`facebook-ads-daily-brief`、`facebook-ads-optimization` 和
`facebook-ads-change-management`。该例外只覆盖 fixture-only 指令、确定性输入
preflight 和只读草稿契约；不得执行 Skill、运行测试、配置模型或外部环境、读取真实
数据、连接 Web/Meta/MCP、持久化、提交审批、执行变更、部署、写操作或正式阶段转换。
四个目录在后续验证前只能标记为 `IMPLEMENTED_UNVERIFIED`；当前已完成的本地确定性
验证不得扩大为独立会话、真实账户或外部系统验证。

当前明确授权的 Skill 开发队列已完成。配置、自动测试、会话验证、全仓回归和 CI 调整
原为 `NOT_RUN`。项目负责人此前要求开发结束后统一配置与测试，本次持续目标现已发起
本地验证阶段：只允许固定 fixture validator、Skill 结构校验、确定性正反测试、输出
契约评分、文档检查、构建、全仓回归及相同命令的 CI 配置。不得自动启动独立模型会话、
调用模型 API、读取真实数据、连接 Web/Meta/MCP、持久化运行输入或结果、部署或执行
写操作。固定 fixture 测试通过后只能标记为 `LOCAL_FIXTURE_VALIDATED`；独立会话未实际
执行时必须保持 `NOT_RUN`，已执行时也只能引用对应 Case 的有限 `PASS`，不得简写为通用
`VALIDATED`。

负责人随后允许继续剩余开发任务。该例外只允许把固定 fixture Creative→Campaign
Builder 与 Optimization→Change Management 组合为人工交接 bundle，并验证阶段、范围、
精确字段传递、人工输入和安全失败；不得自动调用 Skill、启动模型会话、连接 Web、MCP、
Meta 或 Cloudflare、持久化、提交审批、部署、执行写操作或正式阶段转换。Daily Brief
保持只读终点，Analysis 不得在没有独立授权和证据契约时自动进入 Optimization。

## 文档规则

- 按 [`docs/standards/documentation.md`](docs/standards/documentation.md)维护元数据、
  状态、ID、链接和权威边界。
- 当前授权值只能链接状态文档，不能复制成第二份事实源。
- 需求变化同步更新验收条件和追踪矩阵；架构变化使用 ADR。
- 产品发现未知事实写 `UNRESOLVED` 并关联 `DQ-*`；原 Phase 0 业务事实关联
  `BQ-*`，不得猜测默认值。
- 原始访谈、姓名、联系方式、录音和客户数据不得进入仓库，只保存使用参与者别名的
  脱敏证据。
- 不得写入 Token、账号凭据、Authorization Header、客户数据或未脱敏生产响应。

## 安全不变量

- 不得把 Token、密钥、个人信息、客户数据或生产响应写入仓库。
- 不信任模型或客户端传入的身份、账户、对象、权限或角色。
- 未经项目负责人明确授权，不得访问外部资源、部署或执行 Meta 写操作。
- 候选架构不得削弱 [`SECURITY.md`](SECURITY.md)；实现型安全控制只有在产品与架构
  接受后才能成为实施要求。
