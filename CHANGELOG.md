# 变更日志

本项目的重要文档和实现变化记录在此。项目尚未开始业务运行时实现，当前版本保持
`1.0.0`。

## [1.0.0] - 2026-08-05

### Added

- 按项目治理、需求、技术、规范、ADR、规划、Runbook 和模板组织的模块化文档体系。
- 稳定的 `FR-*`、`NFR-*`、`SEC-*` 和 `BQ-*` 标识及需求追踪矩阵。
- Product Discovery 文档、`DQ-01`–`DQ-08`、脱敏 `EVD-*` 证据契约和 `DG0`。
- 面向负责人产品决策、代表性场景和正确产品形态评审的产品定义模板。
- Meta 广告全流程 Web 运营平台与前期 Codex 辅助形态设计。
- 使用固定虚构数据的本地 React/Vite 交互原型，覆盖总览、素材、广告创建与管理、
  数据分析、测试优化及治理。
- `SC-01`–`SC-03` 的 Codex 上下文包、结构化输出样例和聚焦 Skill 契约。
- 桌面与移动端概念图、浏览器验收截图和视觉一致性记录。
- 原型单元测试、类型检查、生产构建和统一 `npm run check` 命令。
- 独立的 ADR-001–ADR-006 文件。
- 文档元数据、权威、状态、Git、测试、安全和发布规范。
- 本地与 GitHub 文档质量检查。
- Phase 0 本地忽略配置模板、离线配置检查、固定 GET allowlist 的 Meta 只读验证工具、
  安全失败测试和独立 CI。
- Meta 只读连接验证 Runbook，覆盖后续本地填值、单独授权、脱敏证据和临时 Token 撤销。
- 默认不可部署的 Phase 1 离线控制平面脚手架，包含 TypeScript Worker、生成的绑定类型、
  Workspace 作用域 D1 migration、固定虚构 fixture、Workers runtime 测试和独立 CI。
- 仅限本机和虚构数据的离线只读分析切片，覆盖账户列表、日期校验、指标汇总、口径上下文
  和安全失败。
- 固定虚构数据的离线周期对比与诊断切片，覆盖完整日级范围、指标变化、零基线、口径
  兼容和不含因果断言的确定性模式。
- 本地 Web 数据分析接入切片，通过固定 Vite 开发代理展示 fixture 周期变化、口径、
  覆盖、错误状态和非因果诊断。
- 离线 Web 账户上下文切片，从固定 Workspace 读取最多 10 个 fixture 账户，只允许列表
  选择，并校验所选账户、币种、时区和请求周期与 comparison 响应一致。
- 离线广告对象层级切片，使用顺序 D1 migration、固定 fixture 和只读 `/objects` 路由
  验证 Campaign、Ad Set、Ad 父级关系，并在 Web 中提供不改变账户级指标的导航。
- 离线对象级分析切片，为已验证 fixture Campaign、Ad Set、Ad 增加三日指标、层级汇总
  一致性、对象 comparison、严格范围绑定和 Web 显式加载，同时保留账户级分析。
- 离线直接子对象拆解切片，为 fixture Campaign 展示直接 Ad Set、为 Ad Set 展示直接
  Ad，验证两期四项可加指标与父对象对账，并明确不排名、不设阈值和不作因果解释。
- 离线对象日趋势切片，为已验证 fixture Campaign、Ad Set、Ad 提供连续 3–31 日输入、
  固定 9 项指标、完整覆盖与口径校验，以及不重新请求的 Web 单指标切换和精确日值表。
- 离线直接子对象日趋势切片，为 fixture Campaign→Ad Set 与 Ad Set→Ad 提供固定
  多序列日值、稳定 ID 顺序、每日四项可加指标父子对账和不重新请求的 Web 指标切换。
- 离线数据质量报告切片，对账户与全部 fixture 对象执行主体日唯一性、连续覆盖、统一
  上下文、层级完整和三层逐日汇总核验，并在 Web 中只展示全量通过证据。
- 离线分析质量 preflight 切片，用仅存于页面内存的通过凭证约束账户、对象、直接子对象
  comparison 与两类趋势，并拒绝超范围请求或口径、快照错配响应。
- 离线 Codex 分析证据包切片，从当前 preflight 支持的 fixture comparison 或直接子对象
  拆解派生确定性只读 JSON，分层记录事实与未知项，并明确不持久化、不上传、不生成建议。
- 离线 Codex 趋势证据包切片，将 schema 更新为 v2，并从当前可信对象或直接子对象趋势
  派生完整固定 9 项日值；直接子对象保持稳定 ID 和逐日父子对账，不解释或排名趋势。
- 项目级 `facebook-ads-analysis` Skill，仅显式校验 schema v2 fixture 手动输入并输出
  带证据路径的 `FACT`、有限非因果 `INFERENCE` 和保留原项的 `UNKNOWN` 只读草稿。
- 离线 Codex 分析草稿评测切片，为五种 analysis kind 提供固定黄金场景、精确证据原值
  契约、8 项确定性评分和安全拒绝回归，不调用模型或外部工具。
- 离线 Codex 独立会话前向评测，为五种 analysis kind 提供不含黄金草稿的会话包、手动
  协议和确定性结果评分；最终 5/5 Case 均在独立会话中获得 8/8。
- 离线 `facebook-ads-creative` 开发切片，提供显式调用 Skill、fixture-only 素材输入
  schema、权利/来源 preflight、创意简报、文案变体与视觉方向草稿契约。
- 离线 Campaign Builder、Daily Brief、Optimization 和 Change Management 开发切片，
  分别提供 schema v1、确定性 preflight 与三层广告草稿、事实简报、证据化非执行优化
  候选和不可执行变更草稿契约。
- 五个上述 Skill 的固定正反 fixture、CLI 安全失败测试、十一组黄金草稿与统一 8 项
  输出契约评分器；本地状态为 `LOCAL_FIXTURE_VALIDATED`。
- 五个 Creative/工作流 Skill 的无黄金答案独立会话包、文件隔离协议、仅标准输入评分器
  和专项回归；最终 5/5 Case 均为 8/8，三项首次拒绝经契约收紧后由全新会话复测通过。
- 统一 `skill:workflow:check`、扩展后的 `skill:eval` / `skill:check`，以及覆盖六个项目级
  Skill 目录和评测材料的 GitHub Actions 路径。
- 两条 fixture-only 跨 Skill 手动工作流，验证 Creative→Campaign Builder 与
  Optimization→Change Management 的范围、精确字段映射、人工输入、非持久化和无外部
  写入边界；包含 10 项评分、11 项专项回归和 `skill:cross-workflow:check`。
- 已批准的根目录安全策略。

### Changed

- 文档入口改为 `docs/README.md`，各领域分别维护唯一事实源。
- 当前阶段和授权集中到项目状态文档。
- 增加独立的 `meta_read_validation_authorized` 授权事实；默认关闭，真实请求前必须明确开启，
  验证结束后必须恢复关闭。
- 架构、路线图、测试和安全内容拆分到对应领域。
- 具体 Cloudflare 账户事实改由 Phase 0 确认，仓库只保留逻辑名。
- 当前状态推进为 `READY_FOR_PHASE_0` / `PRODUCT_DISCOVERY_COMPLETE`。
- 产品定位确认为通用 Meta 广告全流程 Web 运营平台，不做个人定制。
- 前期 AI 助手确认为独立 Codex 会话，使用上下文、信息文档和 Skills；Web 前期不内置
  聊天入口。
- 产品发现改为负责人结构化决策与代表性场景评审，不要求额外用户访谈。
- 所有负责人决定明确标记为尚未经过真实用户或市场验证。
- 项目负责人默认验收完整 Web 原型与 Codex 辅助流程，关闭 `DQ-08`，将 `DG0`
  评估为 `PASS`；该结论不代表真实用户、市场、效率或广告效果验证。
- Phase 0 文档确认已启动并完成 `BQ-01`–`BQ-12`；G0 经评估为 `PARTIAL`。
- `BQ-01`–`BQ-12` 已全部回答；G0 仍等待单独授权后的 Meta 只读接入与外部账户事实
  验证。
- 分析口径改为由广告账户和广告对象上下文动态决定，不再使用项目级投放目标、KPI、
  转化事件、币种、时区、归因或回填天数默认值。
- 确认持久化分析数据和分层保留政策：标准化指标与分析结果 25 个月、原始响应 90 天、
  同步日志 180 天、审计快照 24 个月、临时导出 30 天。
- 首期角色收敛为 `OWNER`、`EDITOR`、`VIEWER`，只有 `OWNER` 可最终审批；网页登录
  使用 Cloudflare Access 且仅允许项目负责人。
- `cf-primary` 确认为项目负责人控制的唯一逻辑 Cloudflare 主账户；实际账户、域名和
  套餐仍待单独授权后验证，ADR-005 继续保持 `DRAFT`。
- Web 与 Codex 的产品职责已确认；MVP 需求映射完成，保留既有 ID 并新增
  `FR-017`–`FR-022`，42 项需求中 32 项为 `ACCEPTED`、10 项保持 `DRAFT`。
- ADR-001、ADR-002、ADR-004、ADR-006 已完成替代方案和可逆性评审并改为
  `ACCEPTED`；Remote MCP 边界 ADR-003 和单一控制平面 ADR-005 继续为 `DRAFT`。
- 接受需求和 ADR 不改变 G0、运行时、Cloudflare/Meta 访问、部署或 Meta 写操作授权；
  未接受技术文档、MCP、自动化和外部写能力继续为候选方案。
- 原 Phase 0 改为依赖 `DG0`，不再承担目标用户、产品形态和 MVP 定义。
- Creative、Campaign Builder、Daily Brief、Optimization 和 Change Management 从开发期
  `IMPLEMENTED_UNVERIFIED` 更新为 `LOCAL_FIXTURE_VALIDATED`；该状态不代表模型、真实
  数据、Meta 政策、审批或执行能力通过。
- Analysis Skill 的自然语言约束明确禁止复述结构化安全布尔值；两项首次前向评测拒绝
  保留后，受影响 Case 均在新的无历史会话中重测通过。

### Removed

- 删除旧单体执行规范及重复的派生架构和路线图，不保留兼容副本。
- 删除被负责人拒绝的三形态静态故事板、评分器和专用测试；Git 历史保留其变更记录。

### Authorization

- 本版本已完成 Product Discovery 并通过 `DG0`，Phase 0 仅启动文档确认。
- 本版本包含只使用固定虚构数据的本地产品原型，不包含业务运行时代码、Cloudflare
  部署、真实 Meta/Cloudflare 访问或 Meta 写能力。
- Phase 0 只读工具当前仅完成凭据无关准备，未填入真实用户信息，也未执行外部请求或
  形成 G0 通过证据。
- 项目负责人决定暂时跳过真实配置，G0 保持 `PARTIAL`；仅额外授权离线脚手架，不授权
  Cloudflare/Meta 访问、资源创建、部署或广告写操作。
- 项目负责人随后允许继续离线只读分析切片；该决定不接受候选 API、架构或需求。
- 项目负责人允许继续离线周期诊断切片；G0 仍为 `PARTIAL`，真实数据、业务阈值、因果
  结论、自动优化和任何外部权限仍未授权。
- 项目负责人允许继续离线 Web 分析接入切片；该决定不启动正式 Phase 3，也不授权
  远程接口、认证替代、真实数据、部署或写操作。
- 项目负责人允许继续离线 Web 账户上下文选择切片；该决定不接受候选需求，不提供手工
  账户 ID，也不授权真实账户、外部访问、部署或写操作。
- 项目负责人允许继续离线广告对象层级切片；该决定不接受候选需求，不提供手工对象 ID
  或对象级指标，也不授权真实同步、外部访问、部署或写操作。
- 项目负责人允许继续离线对象级分析切片；该决定只覆盖固定对象指标、汇总一致性和本机
  显式对比，不接受候选需求、阈值或因果结论，也不授权真实同步、外部访问、部署或写操作。
- 项目负责人允许继续离线直接子对象拆解切片；该决定只覆盖固定直接子级、两期变化和
  父子对账，不接受候选需求、对象排名、业务阈值或因果结论，也不授权真实同步、外部
  访问、部署或写操作。
- 项目负责人允许继续离线对象日趋势切片；该决定只覆盖完整、口径一致的固定 fixture
  日值和本地单指标展示，不接受候选需求、趋势解释、排名、阈值、因果结论或执行建议，
  也不授权真实同步、外部访问、部署或写操作。
- 项目负责人允许继续离线直接子对象日趋势切片；该决定只覆盖已验证 Campaign 或
  Ad Set 的固定子级日值与每日父子对账，不接受候选需求、排名、赢家判断、趋势解释、
  阈值、因果结论或执行建议，也不授权真实同步、外部访问、部署或写操作。
- 项目负责人允许继续离线数据质量报告切片；该决定只覆盖固定 fixture 的完整性、
  一致性与层级汇总检查，不接受候选需求、效果评分、业务阈值、因果结论或 Gate 证据，
  也不授权真实同步、外部访问、部署或写操作。
- 项目负责人允许继续离线分析质量 preflight 切片；该决定不增加 Worker API，不把质量
  通过当作认证、真实授权或 Gate 证据，也不授权真实数据、外部访问、部署或写操作。
- 项目负责人允许继续离线 Codex 分析证据包切片；该决定只覆盖当前可信 fixture 结果的
  手动输入 JSON，不接受 `FR-005`、分析结论或建议，也不授权持久化、自动传输、真实
  数据、外部访问、部署或写操作。
- 项目负责人允许继续离线 Codex 趋势证据包切片；该决定只扩展当前可信 fixture 趋势的
  schema v2 手动输入，不接受 `FR-005`、`NFR-002`、趋势解释、排名、分析结论或建议，
  也不授权持久化、自动传输、真实数据、外部访问、部署或写操作。
- 项目负责人允许继续离线 Codex 分析 Skill 切片；该决定只覆盖 schema v2 fixture 的
  显式本地校验与分层只读草稿，不接受 `FR-005`、`NFR-002` 或候选架构，也不授权
  真实数据、隐式触发、优化动作、外部访问、自动交接、部署或写操作。
- 项目负责人允许继续离线 Codex 分析草稿评测切片；该决定只覆盖五种固定 fixture
  黄金场景、精确输出契约和本地确定性评分，不接受 `FR-005`、`NFR-002` 或候选架构，
  也不授权模型 API、真实数据、外部访问、自动交接、部署或写操作。
- 项目负责人允许继续离线 Codex 独立会话前向评测准备切片；该决定只覆盖五个无期望
  答案测试包和手动结果评分器，不授权自动启动模型、模型 API、持久化结果、真实数据、
  外部访问、自动交接、部署或写操作。
- 项目负责人随后明确授权一次五用例独立会话执行；完成 5 个首次会话和 2 个失败修正后
  的全新重测会话后，该一次性授权已关闭。协议声明未被技术独立验证，结果不形成通用
  模型、真实数据、产品需求或 Gate 证据。
- 项目负责人要求先继续全部开发任务，将配置与测试放到开发结束后；当前决定只授权
  fixture-only `facebook-ads-creative` 的指令、schema、确定性 preflight 与只读草稿
  契约开发，不授权执行测试、配置外部环境、搜索/生成真实素材、连接外部系统或写操作。
- 项目负责人随后明确批准在同一边界内开发 Campaign Builder、Daily Brief、Optimization
  和 Change Management；该决定不授权执行 Skill、运行测试、配置模型或外部环境、读取
  真实数据、连接 Web/Meta/MCP、持久化、提交审批、执行变更、部署或写操作。
- 开发队列完成后，项目负责人批准本地固定 fixture 验证；结构、正反输入、文件安全、
  输出契约、文档、Phase 0、Worker、Web 原型和 production build 回归均通过。
- 项目负责人随后一次性授权五个 Creative/工作流 Skill 的隔离会话评测；五个首次会话
  两项通过、三项拒绝，三项在 Skill 修正后由全新会话获得 8/8。一次性授权已经关闭；
  协议声明未被技术独立验证，结果不形成通用模型、真实数据、产品需求或 Gate 证据。
- 项目负责人随后允许继续 fixture-only 跨 Skill 手动工作流开发与本地校验；该决定只
  覆盖两条固定 bundle，不授权模型会话、Web/MCP/Meta 集成、真实数据、持久化、审批、
  部署或写操作。
