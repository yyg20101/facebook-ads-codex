# Copy this file to config/p0-readiness.local.env and set mode 600.
# Never commit the local file or send its values through chat.
META_GRAPH_API_VERSION=v25.0
META_APP_ID=REPLACE_LATER
META_ALLOWED_AD_ACCOUNT_IDS=act_REPLACE_LATER
META_ACCESS_TOKEN=REPLACE_LATER

# These are dashboard-confirmed facts only. No Cloudflare API token is used.
CLOUDFLARE_ACCOUNT_ID=REPLACE_LATER
CLOUDFLARE_ZONE_ID=OPTIONAL_LATER
CLOUDFLARE_TARGET_DOMAIN=REPLACE_LATER
CLOUDFLARE_PLAN=REPLACE_LATER
