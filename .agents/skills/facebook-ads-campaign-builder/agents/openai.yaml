interface:
  display_name: "Facebook Ads Campaign Builder"
  short_description: "校验离线投放配置并生成 Campaign、Ad Set 与 Ad 草稿"
  default_prompt: "Use $facebook-ads-campaign-builder to turn this offline fixture context into a reviewable Campaign, Ad Set, and Ad draft."
policy:
  allow_implicit_invocation: false
